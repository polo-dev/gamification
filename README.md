# POC — Page "Mes Badges" Wengo

Maquette statique de la page de progression des badges client.

## Accès

La page est déployée automatiquement via GitLab Pages à chaque push sur `main`.

## Structure

```
public/
  index.html   ← la page complète (HTML + CSS + JS inline)
.gitlab-ci.yml ← pipeline de déploiement Pages
```
